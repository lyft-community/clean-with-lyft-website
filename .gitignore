# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.tmp

# OneDrive conflict copies
*conflicted copy*

# Local secrets - never commit
.env
.env.*

# Vercel
.vercel

# Local-only deployment notes (never push - contains internal setup info)
DEPLOYMENT.md
